import { Home } from '@/components/Home';

export const metadata = {
  title: 'Time cards',
  description: 'Time cards',
};

const Page = () => {
  return (
    <main>
      <Home />
    </main>
  );
};

export default Page;
